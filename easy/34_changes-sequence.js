export const changesSequence=(arr)=>{
    let val=0;
         for (let i = 0; i < arr.length; i++) {
            if(arr[i]<0)  {
                return arr.indexOf(arr[i])
                
         }
    }
}